export function Footer() {
  return (
    <footer className="border-t border-line">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-8 text-sm text-ink-faint sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p>Understand Code. Visualize Logic. Learn Better.</p>
        <p>Local-first by design — most of CodeMentor runs without calling an AI at all.</p>
      </div>
    </footer>
  );
}
